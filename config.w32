// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("safenet", "for safenet support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("safenet", "enable safenet support", "no");

if (PHP_SAFENET != "no") {
	EXTENSION("safenet", "safenet.c");
}

